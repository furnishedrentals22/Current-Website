# Test Credentials

## App Password
- **Password**: `emergeontop`
- **Usage**: Required to access admin dashboard at `/`. Also used for admin pricing section on `/listings` page.

## Hard PIN (for admin-protected features)
- **High Sensitivity PIN (admin)**: `3401` (hard-coded, cannot be changed)
- **Low Sensitivity PIN**: `1111` (configurable via PIN Settings)
- **Medium Sensitivity PIN**: `2222` (configurable via PIN Settings)
- **Usage**: High PIN required to view/edit admin door codes, unlock High sensitivity login accounts, and access PIN Settings. Low/Medium PINs required to unlock their respective sensitivity level accounts.

## Public Access
- **Listings Page**: `/listings` - No password required (publicly accessible)
